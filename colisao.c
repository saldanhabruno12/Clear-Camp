#include "colisao.h"
#include <stdlib.h>
#include <stdio.h>
#include <math.h>

//bool colidiu(Entidade* a, Entidade* b) {
//    return (
//        a->x < b->x + b->box.w &&
//        a->x + a->box.w > b->x &&
//        a->y < b->y + b->box.h &&
//        a->y + a->box.h > b->y
//        );
//}
